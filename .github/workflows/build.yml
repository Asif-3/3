name: Python application

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    # Checkout the code from GitHub repository
    - name: Checkout code
      uses: actions/checkout@v2

    # Set up Python environment
    - name: Set up Python 3.x
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    # Install dependencies
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # Set environment variable to disable touch and run Kivy in headless mode
    - name: Set Kivy to run in headless mode and disable touch
      run: |
        echo "KIVY_METRICS_DENSITY=1" >> $GITHUB_ENV
        echo "KIVY_METRICS_FALLBACK=1" >> $GITHUB_ENV
        echo "KIVY_METRICS_DENSITY=1" >> $GITHUB_ENV
        echo "KIVY_METRICS_FALLBACK=1" >> $GITHUB_ENV
        export KIVY_METRICS_FALLBACK=1
        export KIVY_METRICS_DENSITY=1
        export KIVY_METRICS_HEADLESS=1  # Run in headless mode, disabling graphics

    # Run the Kivy app (example command to test if it's working)
    - name: Run Kivy app
      run: |
        python calculator_app.py
