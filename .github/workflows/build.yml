name: Toga Calculator CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          python3-dev \
          python3-gi \
          python3-gi-cairo \
          python3-cairo \
          python3-setuptools \
          libcairo2-dev \
          libgirepository1.0-dev \
          gir1.2-gtk-3.0 \
          pkg-config \
          libgtk-3-dev \
          libgdk-pixbuf2.0-dev \
          libpango1.0-dev \
          libglib2.0-dev \
          gir1.2-glib-2.0 \
          gir1.2-cairo-1.0 \
          gobject-introspection \
          build-essential \
          cmake \
          ninja-build \
          git \
          meson

    - name: Update pip and install build tools
      run: |
        python -m pip install --upgrade pip setuptools wheel
        pip install meson meson-python

    - name: Install PyGObject dependencies
      run: |
        pip install --use-pep517 pycairo
        pip install --use-pep517 pygobject

    - name: Install Toga
      run: |
        pip install toga briefcase

    - name: Run calculator app
      run: python calculator_app.py